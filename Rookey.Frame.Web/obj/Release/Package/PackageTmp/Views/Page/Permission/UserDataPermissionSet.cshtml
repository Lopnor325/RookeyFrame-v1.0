@{
    ViewBag.Title = "UserDataPermissionSet";
}
@using Rookey.Frame.Common;
@using Rookey.Frame.UIOperate;
@{ UIFrameFactory frameFactory = UIFrameFactory.GetInstance(Request);
   string moduleName = Server.UrlDecode(Request["moduleName"].ObjToStr());
   Guid userId = Request["userId"].ObjToGuid();
   int type = Request["type"].ObjToInt();}
@Html.Raw(frameFactory.GetUserDataPermissionSetHTML(moduleName, userId, type))
