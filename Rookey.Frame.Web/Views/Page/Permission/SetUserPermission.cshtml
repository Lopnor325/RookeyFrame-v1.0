@{
    ViewBag.Title = "SetUserPermission";
}
@using Rookey.Frame.Common;
@using Rookey.Frame.UIOperate;
@{ UIFrameFactory frameFactory = UIFrameFactory.GetInstance(Request);
   Guid? userId = Request["userId"].ObjToGuidNull();}
@Html.Raw(frameFactory.GetSetUserPermissionHTML(userId))
